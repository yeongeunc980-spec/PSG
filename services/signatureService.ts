export const convertSignatureToBase64 = (signatureData: string): string => {
  return signatureData;
};

export const saveSignatureImage = async (base64Data: string): Promise<string> => {
  return base64Data;
};
